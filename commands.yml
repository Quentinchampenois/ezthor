name: osp
description: This is my CLI description
commands:
  server:
    cmd: run server
    description: Run the server
  start:
    cmd: start!
    description: Just starts...
    param:
      - name: "name"
      - name: "port"
      - name: "https"
        default: nil
      - name: "watch"
        default: true
      - name: "start"
        default: ""
